# /etc/nginx/sites-available/migravoice.com.conf
# MigraVoice Web Softphone - Production nginx config
# 
# This serves:
#   - / (root): Marketing site or redirect
#   - /app: Web Softphone SPA

server {
    if ($host = migravoice.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    listen [::]:80;
    server_name migravoice.com www.migravoice.com;
    include /etc/nginx/snippets/tenant-suspension.conf;
    
    # Redirect HTTP to HTTPS
    return 301 https://$host$request_uri;


}

server {
    listen 443 ssl http2;
    include /etc/nginx/snippets/ocsp-off.conf;
    listen [::]:443 ssl http2;
    server_name migravoice.com www.migravoice.com;
    include /etc/nginx/snippets/tenant-suspension.conf;

    # SSL certificates (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/migravoice.com-0001/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/migravoice.com-0001/privkey.pem; # managed by Certbot
    ssl_trusted_certificate /etc/letsencrypt/live/migravoice.com/chain.pem;

    # SSL settings
    

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Root for marketing site (optional - can be a simple landing page)
    root /var/www/migravoice.com/public;
    index index.html;

    # --- API proxy to voice-backend (port 3003) ---
    location /auth/ {
        proxy_pass http://127.0.0.1:3003/auth/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /webhooks/ {
        proxy_pass http://127.0.0.1:3003/webhooks/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /billing/ {
        proxy_pass http://127.0.0.1:3003/billing/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /stripe/ {
        proxy_pass http://127.0.0.1:3003/stripe/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /api/cross-platform/ {
        proxy_pass http://127.0.0.1:3003/api/cross-platform/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /api/marketing-provision/ {
        proxy_pass http://127.0.0.1:3003/api/marketing-provision/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /client/ {
        proxy_pass http://127.0.0.1:3003/client/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /voice/ {
        proxy_pass http://127.0.0.1:3003/voice/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Marketing site root
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Web Softphone SPA at /app
    location /app {
        alias /var/www/migravoice.com/app;
        try_files $uri $uri/ /app/index.html;

        # Cache static assets
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    # Health check
    location /health {
        return 200 'OK';
        add_header Content-Type text/plain;
    }

    # Deny hidden files
    location ~ /\. {
        deny all;
    }

    # Logging
    access_log /var/log/nginx/migravoice.com.access.log;
    error_log /var/log/nginx/migravoice.com.error.log;

}
