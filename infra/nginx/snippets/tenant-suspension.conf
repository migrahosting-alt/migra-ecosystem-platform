# Tenant suspension enforcement
# Requires: map $host $tenant_suspended_map in http {}

set $tenant_suspended $tenant_suspended_map;

# Allow ACME challenges even when suspended
if ($request_uri ~* "^/\\.well-known/acme-challenge/") {
    set $tenant_suspended 0;
}

if ($tenant_suspended) {
    return 503;
}