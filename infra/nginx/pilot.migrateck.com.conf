server {
    listen 80;
    server_name pilot.migrateck.com;

    # Redirect HTTP → HTTPS (certbot will add the SSL block)
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name pilot.migrateck.com;

    # Certbot will fill these in
    # ssl_certificate /etc/letsencrypt/live/pilot.migrateck.com/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/pilot.migrateck.com/privkey.pem;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # Next.js static assets (long cache)
    location /_next/static/ {
        proxy_pass http://100.119.105.93:3399;
        proxy_cache_valid 200 365d;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # Next.js frontend (everything else)
    location / {
        proxy_pass http://100.119.105.93:3399;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
