# SSH Access Runbook

- Use host aliases: `pve`, `srv1-web`, `migrapanel-core`, `db-core`, `cloud-core`, `dns-mail-core`, `voip-core`
- Legacy names `vps-core`, `mail-core`, and `dns-core` should be treated as `dns-mail-core` in current operations docs.
- Validate: `ssh pve 'hostname; pveversion'`
