# Tenant Routing Contract (SSOT)

This file maps:
domain → nginx vhost → upstream → pod IP → expected health

> Generated/maintained by MigraAgent after scans and routing changes.

## Template
- tenant:
- domain:
- vhost:
- upstream:
- pod:
- health:
